import React, { FC } from "react";
import { Course } from "../../../types";

const ClassesInfo: FC<{data: Course}> = ({data}) => {

    return (
        <div>

        </div>
    )

};

export default ClassesInfo;